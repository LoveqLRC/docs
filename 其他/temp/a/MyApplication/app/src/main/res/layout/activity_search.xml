<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context="com.example.administrator.myapplication.ui.SearchActivity">
    <!--completionThreshold  属性设置了一个阀值，规定用户打了多少字符之后才出现自动提示，默认值是2，我们在这里改成了1。-->
    <!--dropDownVerticalOffset    下拉的项目选择窗口在垂直方向相对于窗口的偏移量-->
    <!--imeOptions 默认情况下软键盘右下角的按钮为“下一个”，点击会到下一个输入框，保持软键盘
            设置 android:imeOptions="actionDone" ，软键盘下方变成“完成”，
        点击后光标保持在原来的输入框上，并且软键盘关闭。
        android:imeOptions="actionSend" 软键盘下方变成“发送”，点击后光标移动下一个-->


    <AutoCompleteTextView
        android:id="@+id/keyWord"
        android:layout_width="match_parent"
        android:layout_height="36dp"
        android:layout_marginLeft="-10dp"
        android:layout_marginRight="-10dp"
        android:background="@drawable/text_search"
        android:completionThreshold="1"
        android:dropDownVerticalOffset="1.0dip"
        android:focusable="true"
        android:hint="请输入关键字"
        android:imeOptions="actionDone"
        android:inputType="text|textAutoComplete"
        android:maxLength="20"
        android:paddingLeft="20.0dip"
        android:paddingRight="37.0dip"
        android:singleLine="true"
        android:textColor="#000000"
        android:textColorHint="#C0C0C0"
        android:textSize="16.0sp" />


</LinearLayout>
